node_modules/
.DS_Store
*.log
.cache/
**/.cache/

# 环境变量（密钥、数据库连接串）
.env
.env.*
!.env.example
!.env.prod.example

# 曲库由 bun run sync:songs 从上游拉取，不随仓库分发
packages/data/*.json

# 构建产物与编辑器
apps/web/build/
apps/web/.svelte-kit/
.cursor/
backups/
